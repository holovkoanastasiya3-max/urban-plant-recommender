# 🌿 Гайд з міграції проєкту ГрінРеконструкція

## 📁 Структура файлів проєкту

```
green-reconstruction/
│
├── 📄 App.tsx                          # Entry point Figma Make (імпортує з /src/)
├── 📄 index.html                       # HTML entry point
├── 📄 package.json                     # Залежності та скрипти
├── 📄 vite.config.ts                   # Конфігурація Vite
├── 📄 tsconfig.json                    # TypeScript конфігурація
├── 📄 tsconfig.node.json               # TypeScript для Node.js
├── 📄 copy-ui-to-src.bat               # Скрипт для копіювання shadcn UI в /src/
│
├── 📂 src/                             # 🔥 ГОЛОВНА ПАПКА З ЛОГІКОЮ
│   ├── 📄 App.tsx                      # Головний компонент (можна видалити після міграції)
│   ├── 📄 main.tsx                     # React entry point
│   ├── 📄 api.js                       # API logic (можна розширити)
│   │
│   ├── 📂 components/                  # React компоненти
│   │   ├── 📄 Header.tsx               # Хедер з навігацією та логотипом
│   │   ├── 📄 Footer.tsx               # Футер
│   │   ├── 📄 LandingPage.tsx          # Лендінг з контекстом війни/відновлення
│   │   ├── 📄 InputScreen.tsx          # Форма підбору рослин (grid 2 колонки, кастомні слайдери)
│   │   ├── 📄 ResultsScreen.tsx        # Сторінка з результатами (список карток)
│   │   ├── 📄 PlantCard.tsx            # Картка рослини (hover + leaf animation)
│   │   ├── 📄 PlantDetailsScreen.tsx   # Детальна інформація про рослину
│   │   ├── 📄 LeafAnimation.tsx        # 🍃 Анімація листочків при кліку
│   │   └── 📄 LoadingLeaf.tsx          # 🍃 Лоудінг екран з анімацією листочка
│   │
│   └── 📂 styles/
│       ├── 📄 globals.css              # Глобальні стилі + Tailwind v4.0
│       └── 📄 slider.css               # Стилі для кастомних слайдерів з емодзі
│
├── 📂 components/
│   ├── 📂 ui/                          # 🎨 46 shadcn UI компонентів (НЕ ЧІПАТИ!)
│   │   ├── accordion.tsx
│   │   ├── alert-dialog.tsx
│   │   ├── alert.tsx
│   │   ├── ... (всього 46 файлів)
│   │   └── tooltip.tsx
│   │
│   └── 📂 figma/
│       └── ImageWithFallback.tsx       # Системний компонент Figma Make
│
└── 📂 guidelines/
    └── Guidelines.md                   # Документація проєкту
```

---

## 📦 Залежності проєкту

### Основні залежності (dependencies):
```json
{
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "lucide-react": "^0.468.0",        // Іконки
  "react-hook-form": "7.55.0"        // Форми
}
```

### Dev залежності (devDependencies):
```json
{
  "@types/react": "^18.3.18",
  "@types/react-dom": "^18.3.5",
  "@vitejs/plugin-react": "^4.3.4",
  "typescript": "^5.7.3",
  "vite": "^6.0.11",
  "@tailwindcss/vite": "^4.0.0",
  "tailwindcss": "^4.0.0"
}
```

### ⚠️ ВАЖЛИВО: Додаткова бібліотека для анімацій
**Motion (Framer Motion)** - використовується для анімацій листочків і переходів:
```bash
npm install motion
```

**Імпорт у коді:**
```typescript
import { motion, AnimatePresence } from 'motion/react';
```

---

## 🚀 Покрокова інструкція міграції

### Крок 1: Підготовка локального проєкту

```bash
# Створіть нову папку для проєкту
mkdir green-reconstruction
cd green-reconstruction

# Ініціалізуйте Git (опціонально)
git init
```

---

### Крок 2: Копіювання файлів з Figma Make

**📥 Що копіювати:**

#### 1️⃣ Конфігураційні файли (КОРІНЬ проєкту):
- ✅ `package.json`
- ✅ `vite.config.ts`
- ✅ `tsconfig.json`
- ✅ `tsconfig.node.json`
- ✅ `index.html`
- ✅ `copy-ui-to-src.bat` (якщо плануєте використовувати shadcn UI)

#### 2️⃣ Папка `/src/` (ВСЯ ЦІЛКОМ):
```
src/
├── App.tsx                    # Можна видалити, якщо використовуєте тільки /App.tsx
├── main.tsx
├── api.js
├── components/                # Всі 9 компонентів
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── LandingPage.tsx
│   ├── InputScreen.tsx
│   ├── ResultsScreen.tsx
│   ├── PlantCard.tsx
│   ├── PlantDetailsScreen.tsx
│   ├── LeafAnimation.tsx
│   └── LoadingLeaf.tsx
└── styles/
    ├── globals.css
    └── slider.css
```

#### 3️⃣ Entry point файл (КОРІНЬ):
- ✅ `/App.tsx` (використовує імпорти з `/src/`)

#### 4️⃣ Папка `/components/ui/` (якщо потрібні shadcn компоненти):
- ✅ Скопіюйте всю папку `/components/ui/` (46 файлів)
- ✅ Скопіюйте `/components/figma/` (системна папка)

#### 5️⃣ Документація (опціонально):
- `/guidelines/Guidelines.md`
- `/MIGRATION_GUIDE.md` (цей файл)

---

### Крок 3: Встановлення залежностей

```bash
# Встановіть основні залежності
npm install

# ⚠️ ОБОВ'ЯЗКОВО! Встановіть Motion для анімацій
npm install motion

# Перевірте, що все встановилось
npm list motion
# Має показати: motion@11.x.x (або новіша версія)
```

---

### Крок 4: Структура після міграції

Ваш локальний проєкт має виглядати так:

```
green-reconstruction/
├── node_modules/              # Створюється після npm install
├── src/
│   ├── components/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── LandingPage.tsx
│   │   ├── InputScreen.tsx
│   │   ├── ResultsScreen.tsx
│   │   ├── PlantCard.tsx
│   │   ├── PlantDetailsScreen.tsx
│   │   ├── LeafAnimation.tsx         # 🍃 Анімація листочків
│   │   └── LoadingLeaf.tsx           # 🍃 Лоудінг екран
│   ├── styles/
│   │   ├── globals.css
│   │   └── slider.css
│   ├── main.tsx
│   └── api.js
├── components/
│   └── ui/                     # shadcn компоненти (46 файлів)
├── App.tsx                     # Entry point
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── tsconfig.node.json
```

---

### Крок 5: Перевірка імпортів

**❗ КРИТИЧНО ВАЖЛИВО: Шляхи імпортів**

У файлі `/App.tsx` (корінь):
```typescript
import { LandingPage } from './src/components/LandingPage';
import { InputScreen } from './src/components/InputScreen';
import { ResultsScreen } from './src/components/ResultsScreen';
import { PlantDetailsScreen } from './src/components/PlantDetailsScreen';
import { LoadingLeaf } from './src/components/LoadingLeaf';
import { motion, AnimatePresence } from 'motion/react';
```

У файлах `/src/components/*.tsx`:
```typescript
// Імпорт типів з App.tsx (корінь проєкту)
import { FormData, Plant } from '../../App';

// Імпорт інших компонентів
import { Header } from './Header';
import { Footer } from './Footer';

// Анімації
import { motion } from 'motion/react';
import { LeafAnimation } from './LeafAnimation';
```

---

### Крок 6: Запуск проєкту

```bash
# Запустіть dev сервер
npm run dev

# Vite покаже URL (зазвичай http://localhost:5173)
```

**Що має працювати:**
- ✅ Лендінг з градієнтом та іконками
- ✅ Форма підбору (grid 2 колонки, кастомні слайдери з емодзі)
- ✅ Анімація листочків 🍃 при кліку на кнопки/картки
- ✅ Лоудінг екран з листочком при переходах
- ✅ Плавні переходи між сторінками
- ✅ Hover ефекти на картках

---

## 🎨 Важливі деталі UI

### Кольорова схема:
- **Акцентний колір (шавлія):** `#A9B89E`
- **Фон:** Градієнти `from-stone-50 via-stone-100 to-stone-50`
- **Текст:** `text-slate-800`, `text-slate-600`
- **Бордери:** `border-stone-200`, `border-stone-300`

### Анімації:
1. **LeafAnimation.tsx** - 6 листочків розлітаються вгору при кліку
2. **LoadingLeaf.tsx** - листочок гойдається і плаває під час завантаження
3. **Motion transitions** - плавні переходи між екранами (0.3-0.8s)

### Кастомні слайдери:
- Емодзі як повзунок: 🏜️🌾🌵🌿🪴 (посухостійкість)
- Емодзі як повзунок: 🌱🌿🐛🐝🦋 (біорізноманіття)
- Стилі в `/src/styles/slider.css`

---

## 📸 Зображення та SVG

**❗ У проєкті НЕ використовуються зовнішні зображення**

- Всі плейсхолдери рослин: `[Фото рослини]` з емодзі фоном (🌿, 🌳)
- Іконки: lucide-react (вже в залежностях)
- Емодзі: нативні Unicode символи (🍃, 🌿, 🌱, тощо)

---

## 🛠️ Налаштування в Cursor

### 1. Відкрийте проєкт:
```bash
cursor .
```

### 2. Перевірте TypeScript:
- Cursor автоматично підхопить `tsconfig.json`
- Перевірте, що немає помилок у TypeScript

### 3. Налаштуйте ESLint (опціонально):
```bash
npm run lint
```

### 4. Тестування на різних екранах:
- Мобільний: 320px-768px
- Планшет: 768px-1024px
- Десктоп: 1024px+

---

## ⚠️ Поширені проблеми та рішення

### Проблема 1: Motion not found
```bash
# Рішення:
npm install motion
```

### Проблема 2: Tailwind не працює
```bash
# Перевірте, що у vite.config.ts є:
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

### Проблема 3: Помилки імпортів
- Перевірте шляхи: `/src/components/` vs `./components/`
- У `/App.tsx` (корінь): `'./src/components/...'`
- У `/src/components/*.tsx`: `'./ComponentName'` або `'../../App'`

### Проблема 4: Анімації не працюють
- Перевірте, що motion встановлено: `npm list motion`
- Перевірте імпорт: `import { motion, AnimatePresence } from 'motion/react'`

---

## ✅ Чеклист перед запуском

- [ ] Скопійовано всі файли з `/src/`
- [ ] Скопійовано `/App.tsx` (корінь)
- [ ] Скопійовано конфіги (package.json, vite.config.ts, tsconfig.json)
- [ ] Встановлено залежності: `npm install`
- [ ] Встановлено motion: `npm install motion`
- [ ] Перевірено шляхи імпортів у `/App.tsx` та компонентах
- [ ] Запущено проєкт: `npm run dev`
- [ ] Протестовано анімації листочків 🍃
- [ ] Протестовано форму з слайдерами
- [ ] Перевірено responsive дизайн

---

## 📝 Примітки

- **Tailwind v4.0** використовується через `@tailwindcss/vite`
- **Не використовуємо** класи для font-size, font-weight (є в globals.css)
- **shadcn UI компоненти** можна не копіювати, якщо не плануєте розширювати UI
- **Motion** - це нова назва Framer Motion (використовується в проєкті)

---

## 🎯 Наступні кроки (після міграції)

1. Підключити реальний бекенд замість `/src/api.js`
2. Додати фото рослин (замість плейсхолдерів)
3. Розширити базу даних рослин
4. Додати фільтри та сортування на сторінці результатів
5. Реалізувати збереження улюблених рослин (localStorage або бекенд)

---

Створено для безпечної міграції проєкту ГрінРеконструкція 🌿
Версія: 1.0.0 | Дата: 2025
